<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Manage Users | Admin Console</title>
  <link rel="stylesheet" href="/css/style2.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=DM+Mono:ital,wght@0,300;0,400;0,500;1,400&display=swap" rel="stylesheet">
  
  <style>
    /* Admin Table Styles */
    .user-list-card {
        background: var(--card-bg);
        border: 1px solid rgba(255,255,255,0.05);
        border-radius: 20px;
        overflow: hidden;
        margin-top: 2rem;
    }
    
    .cyber-table {
        width: 100%;
        border-collapse: collapse;
        font-family: "DM Mono", monospace;
    }
    
    .cyber-table th {
        background: #000;
        color: var(--brand-blue);
        text-align: left;
        padding: 1rem 1.5rem;
        border-bottom: 2px solid rgba(255,255,255,0.1);
        font-size: 0.9rem;
        text-transform: uppercase;
    }
    
    .cyber-table td {
        padding: 1rem 1.5rem;
        border-bottom: 1px solid rgba(255,255,255,0.05);
        color: var(--text-main);
        vertical-align: middle;
    }
    
    .role-badge {
        padding: 4px 10px;
        border-radius: 4px;
        font-size: 0.8rem;
        font-weight: bold;
        text-transform: uppercase;
        display: inline-block;
    }
    .role-admin { color: #ff4444; border: 1px solid #ff4444; background: rgba(255, 68, 68, 0.1); }
    .role-user { color: #888; border: 1px solid #555; }
    
    .action-btn {
        background: none; border: none; cursor: pointer;
        font-family: "DM Mono"; font-weight: bold;
        padding: 6px 12px; border-radius: 4px; transition: 0.3s;
        font-size: 0.85rem;
    }
    .btn-promote { border: 1px solid #27c93f; color: #27c93f; }
    .btn-promote:hover { background: #27c93f; color: #000; box-shadow: 0 0 10px #27c93f; }
    
    .btn-demote { border: 1px solid #ff4444; color: #ff4444; }
    .btn-demote:hover { background: #ff4444; color: #fff; box-shadow: 0 0 10px #ff4444; }

    /* --- MOBILE RESPONSIVENESS (Card View) --- */
    @media (max-width: 768px) {
        .user-list-card {
            background: transparent;
            border: none;
        }

        /* Hide the Table Header on Mobile */
        .cyber-table thead {
            display: none;
        }

        /* Make Rows look like Cards */
        .cyber-table, .cyber-table tbody, .cyber-table tr, .cyber-table td {
            display: block;
            width: 100%;
        }

        .cyber-table tr {
            margin-bottom: 1.5rem;
            background: var(--card-bg, #1a1a1a); /* Fallback color */
            border: 1px solid rgba(255,255,255,0.1);
            border-radius: 15px;
            padding: 1rem;
            box-shadow: 0 4px 6px rgba(0,0,0,0.3);
        }

        .cyber-table td {
            text-align: right;
            padding: 0.8rem 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(255,255,255,0.05);
        }

        .cyber-table td:last-child {
            border-bottom: none;
            padding-top: 1.5rem;
            justify-content: center; /* Center buttons on mobile */
        }

        /* Inject labels using data-label attribute */
        .cyber-table td::before {
            content: attr(data-label);
            float: left;
            font-weight: bold;
            text-transform: uppercase;
            color: var(--brand-blue, #4876ff);
            font-size: 0.8rem;
        }

        /* Fix long emails breaking layout */
        .cyber-table td[data-label="Email"] {
            word-break: break-all;
            font-size: 0.9rem;
        }
        
        /* Stack buttons if needed */
        .action-btn {
            width: 100%;
            text-align: center;
        }
    }
  </style>
</head>
<body>
  <%- include('../partials/navbar') %>
  
  <main class="main-content">
    <%- include('../partials/flash') %>
    
    <div class="container">
       <div class="discussion-top-bar centered-bar">
        <div class="header-pill admin-pill">
          <h1><i class="fas fa-users-cog"></i> USER PERMISSIONS</h1>
        </div>
      </div>

      <div class="user-list-card">
        <table class="cyber-table">
            <thead>
                <tr>
                    <th>Username</th>
                    <th>Email</th>
                    <th>Current Role</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% users.forEach(user => { %>
                    <tr>
                        <td data-label="Username">
                            <i class="fas fa-user-circle"></i> <%= user.username %>
                            <% if(user.username === 'bhavi12z') { %> 
                                <i class="fas fa-crown" style="color: gold; margin-left:5px;"></i> 
                            <% } %>
                        </td>
                        
                        <td data-label="Email"><%= user.email %></td>
                        
                        <td data-label="Current Role">
                            <span class="role-badge <%= user.role === 'admin' ? 'role-admin' : 'role-user' %>">
                                <%= user.role %>
                            </span>
                        </td>
                        
                        <td data-label="Actions">
                            <% if (user.username !== 'bhavi12z') { %>
                                
                                <% if (user.role === 'admin') { %>
                                    <form action="/admin/demote/<%= user._id %>" method="POST" style="width: 100%;">
                                        <button class="action-btn btn-demote">
                                            <i class="fas fa-arrow-down"></i> REVOKE
                                        </button>
                                    </form>
                                <% } else { %>
                                    <form action="/admin/promote/<%= user._id %>" method="POST" style="width: 100%;">
                                        <button class="action-btn btn-promote">
                                            <i class="fas fa-arrow-up"></i> PROMOTE
                                        </button>
                                    </form>
                                <% } %>

                            <% } else { %>
                                <span style="color:var(--text-muted); font-size: 0.8rem; letter-spacing: 1px;">SUPER_USER</span>
                            <% } %>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
      </div>
      
      <div style="text-align: center; margin-top: 2rem;">
        <a href="/admin/addManga" class="btn btn-outline">Go to Add Manga</a>
      </div>
      
    </div>
  </main>
  
  <%- include('../partials/footer') %>
</body>
</html>